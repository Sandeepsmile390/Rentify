# Mobile App Environment Variables — Example
# Copy this to .env and set your values.
# Expo exposes only variables prefixed with EXPO_PUBLIC_ to the app bundle.
EXPO_PUBLIC_API_URL=http://localhost:5000/api
